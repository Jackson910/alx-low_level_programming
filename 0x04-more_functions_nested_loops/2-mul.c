#include "main.h"
/**
 * mul -  multiplies two integers.
 * @x: The first number in multiply
 * @y: The second number in multiply
 * Return: The result of multiplication between x param and y param
 */
int mul(int x, int y)
{
	return (x * y);
}
